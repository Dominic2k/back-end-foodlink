spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/foodlink_db?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

app:
  jwt:
    secret: ${JWT_SECRET:change-me}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}

# TODO: Add environment-specific settings and secrets via env vars.
